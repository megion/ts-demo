" Local project a vimrc file.
set colorcolumn=80

set tabstop=2 " count of space which show tab
set softtabstop=2 " count of space which show tab when it added
set shiftwidth=2 " count of space add by commands '>>' and '<<'
